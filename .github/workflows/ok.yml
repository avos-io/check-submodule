name: all-good-here

on:
  pull_request:
    paths:
      - '**'

jobs:
  all-good-here:
    runs-on: ubuntu-latest

    steps:
      - name: Ok
        run: |
          echo "Ok"
          exit 0

  not-good-here:
    runs-on: ubuntu-latest

    steps:
      - name: Still Ok
        run: |
          echo "Still Ok"
          exit 0
